package {
	
	import pathfinding.Tile;
	
	public class AI {
		
		// get the AI's unit movement order
		public static function getUnitMovementCommand(owner:int, flocks:Vector.<Flock>, mapData:Vector.<Vector.<Tile>>) {
			
		}
		
		// get the AI's build command
		public static function getUnitBuildCommand(owner:int, flocks:Vector.<Flock>, mapData:Vector.<Vector.<Tile>>) {
			
		}
		
	}
	
}